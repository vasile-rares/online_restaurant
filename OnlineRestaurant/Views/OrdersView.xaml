<UserControl
    x:Class="OnlineRestaurant.Views.OrdersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:OnlineRestaurant.Views"
    xmlns:converters="clr-namespace:OnlineRestaurant.Converters"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:OrderStatusToColorConverter x:Key="OrderStatusToColorConverter" />
        <converters:OrderStatusToStringConverter x:Key="OrderStatusToStringConverter" />
        <converters:IdTruncateConverter x:Key="IdTruncateConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Istoricul comenzilor dvs."
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0,0,0,10" />

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                       Margin="0,5" />
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <ListView ItemsSource="{Binding Orders}"
                      BorderThickness="0"
                      Background="Transparent">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDDDDD"
                                BorderThickness="1"
                                CornerRadius="5"
                                Padding="15"
                                Margin="0,0,0,10"
                                Background="White">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock>
                                        <Run Text="Comandă #" />
                                        <Run Text="{Binding IdOrder, Converter={StaticResource IdTruncateConverter}}" />
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text="Data: " />
                                        <Run Text="{Binding OrderDate, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" />
                                    </TextBlock>
                                </StackPanel>

                                <Border Grid.Row="0" Grid.Column="1"
                                        Background="{Binding Status, Converter={StaticResource OrderStatusToColorConverter}}"
                                        Padding="7,3"
                                        CornerRadius="3"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Status, Converter={StaticResource OrderStatusToStringConverter}}"
                                               Foreground="White"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               TextAlignment="Center"
                                               Margin="3,0" />
                                </Border>

                                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,0">
                                    <TextBlock Text="Produse comandate:" FontWeight="SemiBold" Margin="0,0,0,5" />

                                    <ItemsControl ItemsSource="{Binding OrderDishes}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,3">
                                                    <TextBlock Text="{Binding Quantity}" Width="10" />
                                                    <TextBlock Text="x" Margin="0,0,5,0" />
                                                    <TextBlock Text="{Binding Dish.Name}" />
                                                    <TextBlock Text=" - " />
                                                    <TextBlock Text="{Binding Dish.Price, StringFormat='{}{0:0.00} Lei'}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                            Orientation="Horizontal"
                                            HorizontalAlignment="Right"
                                            Margin="0,10,0,0">
                                    <TextBlock Text="Total:" FontWeight="Bold" Margin="0,0,5,0" />
                                    <TextBlock Text="{Binding TotalAmount, StringFormat='{}{0:0.00} Lei'}" FontWeight="Bold" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Reîmprospătează"
                    Command="{Binding RefreshCommand}"
                    Padding="15,5"
                    Margin="0,0,10,0"
                    Background="#2ECC71"
                    Foreground="White"
                    BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4" />
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="Înapoi"
                    Command="{Binding BackCommand}"
                    Padding="15,5"
                    Background="#775B00"
                    Foreground="White"
                    BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4" />
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</UserControl> 